package com.mc.security.jwt.verify;

/**
 * @author Wenyu
 * @since 3/6/17
 */
public class RefreshTokenVerifier {

    // check if access token is expired, throw exception if not

    // check if refresh token is expired, throw exception if yes

    // check if refresh token is in the blacklist, throw exception if yes

    // clean up previous refresh token in the blacklist for the user

    // if access token created successfully, add the refresh token into the blacklist
}
